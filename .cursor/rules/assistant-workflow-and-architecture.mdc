---
description: Persistent backend architect style and scoped delivery workflow
alwaysApply: true
---

# Backend Architect Mode

- Prioritize reliability, security, scalability, and performance.
- Keep clear layering: `transport -> service -> repository`.
- Prefer secure defaults: no hardcoded secrets, explicit migrations, auditable changes.
- Preserve backward compatibility unless a breaking change is explicitly requested.

# Scoped Delivery Workflow

For each user task, always create a feature branch, commit with Conventional Commits, push, and provide a PR link.

PR ownership preference:
- Prepare branches/commits/PRs, but do **not** merge PRs unless the user explicitly asks.
- User preference override: if the user explicitly asks for a **release** (for example: `release`, `релиз`, `сделай релиз`), the agent may complete the PR workflow end-to-end, including merge **after** required CI is green.
- If release intent is not explicit, stop at green CI and hand off the PR link for manual merge by the user.
- In each PR body, include a concise changelog and a `Release notes draft` section.
- Before opening every PR, run `make preprod`.
- After local runtime verification, always stop started containers with `make docker-compose-down`.

Then apply execution scope by task type:

## Code/runtime tasks (application behavior changes)

Run the full cycle:

1. `make preprod`
2. `make docker-compose-up` with startup/log verification
3. `make docker-compose-down`
4. Push branch and provide PR link
5. Wait for GitHub Actions CI to pass (green)
6. Merge the PR only after explicit user confirmation/request
7. Delete local feature branch
8. Sync local `main` (`git fetch --prune origin` + `git pull --ff-only`)
9. Create and push a new annotated tag

## Docs/meta tasks (no runtime behavior change)

Do a lightweight cycle:

1. Run `make preprod`
2. Commit + push + PR link
3. Wait for GitHub Actions CI to pass (green)
4. Merge the PR only after explicit user confirmation/request
5. Delete local feature branch
6. Sync local `main`
7. If any containers were started during checks, run `make docker-compose-down`
8. Skip local runtime checks and skip release tag by default

# Post-Release Verification

- After every release tag or release-triggered deploy, always verify that the GitHub Actions release workflow completed successfully.
- If the release workflow includes deploy steps, always inspect the deploy job result, not just the image build/push step.
- After a successful production deploy, always check startup logs for both the bot container and the database container.
- Treat startup-stage connection errors, restart loops, missing env/config files, and image pull failures as release blockers until verified fixed.
- Do not report a release as successful until Actions status is green and production startup logs show the bot and database reached a healthy running state.

# Git Safety

- Never commit or push directly to `main`.
- Delete only local branches unless explicitly asked otherwise.
- Avoid destructive git operations unless explicitly requested.

# User Progress Reporting

- During multi-step implementation tasks, after each completed step report current completion percentage to the user (e.g., `Progress: 35%`), based on the execution plan for that task.
- If the user explicitly asks for percentage reporting, include a progress percentage update after every operational step until the task is fully complete.
- If the user asks to track a specific RFC section by percent, report that section-level readiness variable from `docs/rfc-todo-status.md` (for example `ready_section_6_online_payments`) instead of per-step percentages.
